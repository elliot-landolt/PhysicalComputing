/**/int spkPin = 12;int freqs[] = {440, 500, 440, 700, 750}; // array (essentially a list)// can put all notes in song ^^ and then use the small code at endint len[] = {500, 500, 500, 1000, 1000, 500};void setup() {    pinMode(spkPin, OUTPUT);    /*    tone(spkPin, 440, 1000); // 1 second tone    delay(1500); // delay so I can hear the tone    tone(spkPin, 600, 1000);    delay(1500);            for(int i=200; 1<=800; i+=100){      tone(spkPin, i, 1000); // tone (pin, freq, duration)      delay(1200);    }    */    for(int i=0; i<5; i++){      tone(spkPin, freqs[i], len[i]);      delay(1200);    }}void loop() {    }